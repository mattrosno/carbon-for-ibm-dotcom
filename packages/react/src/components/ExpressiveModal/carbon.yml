# yaml-language-server: $schema=https://unpkg.com/@carbon-platform/schemas/carbon-resources.schema.json
assets:
  expressive-modal:
    status: stable
    framework: react
    inherits:
      asset: 'ibmdotcom-styles@latest/expressive-modal'
      properties:
        - name
        - description
        - externalDocsUrl
        - thumbnailPath
        - type
        - platform
